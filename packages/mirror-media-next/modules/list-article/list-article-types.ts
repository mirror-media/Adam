/**
 * What the article list needs from a section: the name it prints on the hero
 * image, and the slug that picks the section colour and the GPT page key.
 */
export type ArticleListSection = {
  name: string
  slug: string
}
